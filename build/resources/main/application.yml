server:
  port: 8090

spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  listener:
    default-requeue-rejected: false
    type: direct
  application:
    dlqExchange: deadLetterExchange
    dlqueue: deadLetter